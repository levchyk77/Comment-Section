import { HoveredCommentDirective } from './hovered-comment.directive';

describe('HoveredCommentDirective', () => {
  it('should create an instance', () => {
    const directive = new HoveredCommentDirective();
    expect(directive).toBeTruthy();
  });
});
